using System.Collections.Generic;
using System.Linq;
using Tarahiro;

namespace Item
{

    public class ItemMasterDataUtil
    {
        /*
        public static IReadOnlyDictionary<int, IItemMaster> GetMasterDictionary()
        {
            return ServiceLocator.ResolveOrderedDictionary<IGameIntroductionMasterDataRecord>().Enumerable.Select(data => data.GetMaster()).ToDictionary(master => master.Id, master => master);
        }
        */
    }
}
